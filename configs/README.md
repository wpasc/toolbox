# Configurations

Dotfiles and tool configurations. Organized to mirror actual config locations.

## Structure

```
nvim/    # ~/.config/nvim/
tmux/    # ~/.tmux.conf and related
```

## Usage

To use a config, symlink or copy to the appropriate location. Document any dependencies or required plugins in each tool's subdirectory.
